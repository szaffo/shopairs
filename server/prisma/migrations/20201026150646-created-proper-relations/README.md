# Migration `20201026150646-created-proper-relations`

This migration has been generated by Szabó Martin at 10/26/2020, 4:06:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "name" TEXT,
    "updated_at" DATETIME NOT NULL,
    "created_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
INSERT INTO "new_User" ("id", "email", "password", "name", "updated_at", "created_at") SELECT "id", "email", "password", "name", "updated_at", "created_at" FROM "User";
DROP TABLE "User";
ALTER TABLE "new_User" RENAME TO "User";
CREATE UNIQUE INDEX "User.email_unique" ON "User"("email");
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201026140825-pair-nullable..20201026150646-created-proper-relations
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -14,19 +14,20 @@
   id      Int      @default(autoincrement()) @id
   email   String   @unique
   password String
   name    String?
-  pair    Pair?     @relation(fields: [pairId], references: [id])
-  pairId  Int?
   updated_at DateTime @updatedAt
   created_at DateTime @default(now())
+  createdPair Pair? @relation("CreatorUser")
+  joinnedToPair Pair? @relation("JoinnerUser")
 }
 model Pair {
   id Int @default(autoincrement()) @id
   pair1_id Int
   pair2_id Int?
-  users User[]
+  creator User @relation("CreatorUser", fields: [pair1_id], references: [id])
+  joinner User? @relation("JoinnerUser", fields: [pair2_id], references: [id])
   connection_code String
   updated_at DateTime @updatedAt
   created_at DateTime @default(now())
 }
```


